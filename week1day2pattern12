#include <stdio.h>

int main() {
    int rows=5;
    for (int i = 1; i <= rows; i++) {
        int temp = i;
        for (int j = i; j >= 1; j--) {
            printf("%d ", temp);
            temp = temp + rows;
        }
        printf("\n");
    }

    return 0;
}

